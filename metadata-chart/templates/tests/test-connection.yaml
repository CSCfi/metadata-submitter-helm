apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "metadata-chart.fullname" . }}-test-connection"
  labels:
    {{- include "metadata-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "metadata-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
